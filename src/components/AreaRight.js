import React from "react";
import styled from "styled-components";

const AreaRight = (props) => {
  return (
    <Container>
      <div>Area Rightside</div>
    </Container>
  );
};

const Container = styled.div`
  grid-area: area-right;
`;

export default AreaRight;
